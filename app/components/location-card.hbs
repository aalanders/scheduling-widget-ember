<div class="location-card location-card-{{@index}}">
    <div>
        <h3>{{@office.name}}</h3>
        <p>{{@office.phone}}</p>
    </div>
    {{!-- LinkTo would go here to go to next route and step --}}
    <button type="button" class="button">Select</button>
</div>